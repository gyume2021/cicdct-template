/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package init.test.app;

import init.test.list.LinkedList;

import static init.test.utilities.StringUtils.join;
import static init.test.utilities.StringUtils.split;
import static init.test.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
